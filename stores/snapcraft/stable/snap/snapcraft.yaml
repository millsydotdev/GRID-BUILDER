name: grid
adopt-info: grid
summary: GRID - AI-native code editor
description: |
  GRID is an open-source, AI-native code editor - the open-source alternative to Cursor.

base: core20
grade: stable
confinement: classic
compression: lzo

parts:
  grid:
    plugin: nil
    override-build: |
      set -eu
      # Get .deb url from GRID releases
      wget --quiet https://api.github.com/repos/millsydotdev/binaries/releases -O latest.json
      VERSION=$( jq -r 'sort_by(.tag_name)|last.tag_name' latest.json )
      DEB_URL=$( jq -r 'map(select(.tag_name == "'"${VERSION}"'"))|first.assets[].browser_download_url|select(endswith("'"_${SNAPCRAFT_TARGET_ARCH}.deb"'"))' latest.json )
      DEB_NAME=$( basename "${DEB_URL}" )
      # Downloading .deb
      wget "${DEB_URL}" -O "${SNAPCRAFT_PART_INSTALL}/${DEB_NAME}"
      # Unpacking .deb
      dpkg -x "${SNAPCRAFT_PART_INSTALL}/${DEB_NAME}" "${SNAPCRAFT_PART_INSTALL}"
      # Clean up
      rm -f latest.json
      rm -f "${SNAPCRAFT_PART_INSTALL}/${DEB_NAME}"
      # Set version
      snapcraftctl set-version "${VERSION}"
      # Prepare GUI
      mkdir -p "${SNAPCRAFT_PART_INSTALL}/meta/gui"
      cp "${SNAPCRAFT_PART_INSTALL}/usr/share/grid/resources/app/resources/linux/code.png" "${SNAPCRAFT_PART_INSTALL}/meta/gui/grid.png"
      # Update paths
      sed -i 's|Exec=/usr/share/grid/grid|Exec=grid --force-user-env|g' "${SNAPCRAFT_PART_INSTALL}/usr/share/applications/grid.desktop"
      sed -i 's|Exec=/usr/share/grid/grid|Exec=grid --force-user-env|g' "${SNAPCRAFT_PART_INSTALL}/usr/share/applications/grid-url-handler.desktop"
      sed -i 's|Icon=grid|Icon=${SNAP}/meta/gui/grid.png|g' "${SNAPCRAFT_PART_INSTALL}/usr/share/applications/grid.desktop"
      sed -i 's|Icon=grid|Icon=${SNAP}/meta/gui/grid.png|g' "${SNAPCRAFT_PART_INSTALL}/usr/share/applications/grid-url-handler.desktop"
    build-packages:
      - wget
      - jq
    stage-packages:
      - ca-certificates
      - libasound2
      - libatk-bridge2.0-0
      - libatk1.0-0
      - libatspi2.0-0
      - libcairo2
      - libcanberra-gtk3-module
      - libcurl3-gnutls
      - libcurl3-nss
      - libcurl4
      - libdrm2
      - libgbm1
      - libgl1
      - libglib2.0-0
      - libgtk-3-0
      - libibus-1.0-5
      - libnss3
      - libpango-1.0-0
      - libsecret-1-0
      - libxcomposite1
      - libxdamage1
      - libxfixes3
      - libxkbcommon0
      - libxkbfile1
      - libxrandr2
      - libxss1
      - locales-all
      - packagekit-gtk3-module
      - xdg-utils
    prime:
      - -usr/share/doc
      - -usr/share/fonts
      - -usr/share/icons
      - -usr/share/lintian
      - -usr/share/man
      - -usr/share/grid/chrome-sandbox
    build-attributes:
      - enable-patchelf
    override-prime: |
      set -eux
      snapcraftctl prime
      for snap in "core20"; do
        cd "/snap/${snap}/current/usr/lib/${SNAPCRAFT_ARCH_TRIPLET}"
        find . -type f,l -name "*.so*" -exec bash -c "rm -f ${SNAPCRAFT_PRIME}/{}*" \;
      done

  electron-launch:
    after:
      - grid
    plugin: dump
    source: snap/local/bin

apps:
  grid:
    command: electron-launch $SNAP/usr/share/grid/bin/grid --no-sandbox
    desktop: usr/share/applications/grid.desktop
    common-id: grid.desktop

  url-handler:
    command: electron-launch $SNAP/usr/share/grid/bin/grid --open-url --no-sandbox
    desktop: usr/share/applications/grid-url-handler.desktop
